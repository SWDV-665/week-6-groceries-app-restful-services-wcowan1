import { GROCERYDirective } from './grocery.directive';

describe('GROCERYDirective', () => {
  it('should create an instance', () => {
    const directive = new GROCERYDirective();
    expect(directive).toBeTruthy();
  });
});
